{
  "name": "hooks",
  "version": "0.2.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm,cjs && tsup src/policy.ts src/pretool-guard.ts src/posttool-detect.ts src/stop-checkpoint.ts --format esm --no-dts",
    "dev": "tsx watch src/*.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0"
  }
}