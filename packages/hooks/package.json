{
  "name": "hooks",
  "version": "0.2.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/*.ts --dts --format esm,cjs",
    "dev": "tsx watch src/*.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0"
  }
}